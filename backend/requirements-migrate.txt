# Minimal deps for `alembic upgrade` in the migrate Docker service (no FastAPI stack).
alembic>=1.14.0
sqlalchemy>=2.0.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
